[package]
name = "dims_core"
version = "0.2.1"
authors = ["william"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[features]
default = ["str"]
no_std = []
str = []
f64 = []